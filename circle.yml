general:
  artifacts:
    - "tests/target"

machine:
  python:
  services:
    - docker
  environment:
    CI: true

dependencies:
  pre:
    - pip install mock
  post:
    - docker version
    - docker info

test:
  override:
    - tox

notify:
  webhooks:
    - url: $GITTER_WEBHOOK_URL
